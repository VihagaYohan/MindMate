// components
import { AppContainer, AppText } from '../../components'

const SettingsPage = () => {

    return <AppContainer>
        <AppText text='settings page' />
    </AppContainer>

}

export default SettingsPage